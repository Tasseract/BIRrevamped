// Copy this file to `api-config.js` and set `API_BASE` to your deployed backend URL.
// Example: const API_BASE = 'https://my-backend.up.railway.app';

// Default for local development:
const API_BASE = 'http://localhost:3000';

// If you deploy the frontend as static files, create `api-config.js` (same dir)
// and set the URL of your hosted backend so the UI will call the correct API.
