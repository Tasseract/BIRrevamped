{
  "devDependencies": {
    "dotenv-cli": "^10.0.0",
    "prisma": "^6.18.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prisma": "dotenv -- prisma",
    "start": "node server.js",
    "postinstall": "npx prisma generate"
    ,"generate-api-config": "node scripts/generate-api-config.js",
    "build": "node scripts/generate-api-config.js"
  },
  "dependencies": {
    "@prisma/client": "^6.18.0",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "express": "^5.1.0"
  }
}
